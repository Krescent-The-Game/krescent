# Archived — not deployed

This Vite + BabylonJS 7 box demo was a **stopgap** during Eternal Flame rebirth (commit `4acafc8`). It is **not** the original Krescent game.

**Do not** wire this into `build:play`. The original game is built from `legacy/nuxt/`.

Kept for reference only.
